package com.model.dataset.intf;

public enum DataType {
    STRING,
    INT,
    LENGTH, // rozmiar danych w bajtach
    DOUBLE,
    BOOLEAN,
    DATE,
    ENUM,
    OBJECT,
    ARRAY;
}
